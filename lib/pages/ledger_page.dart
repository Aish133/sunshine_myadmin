import 'package:flutter/material.dart';

class LedgerPage extends StatelessWidget {
  const LedgerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ledger Page", style: TextStyle(fontSize: 24)),
    );
  }
}
