import 'package:flutter/material.dart';

class HrPolicyPage extends StatelessWidget {
  const HrPolicyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("HR Policy Page", style: TextStyle(fontSize: 24)),
    );
  }
}
